# Codex 项目入口

开始任何开发或知识维护任务前，必须阅读并遵守 `KNOWLEDGE_RULES.md`。

## 开发原则

- 本项目为独立、公司自有实现；不得复制 GPL 项目源码。
- 首先保证证据可追溯、状态可恢复和密级策略可靠，再扩展界面。
- 原始资料只能读取；运行期副本写入 `workspace/raw/` 并设置为只读。
- 数据库变更必须通过可重复执行的迁移完成。
- 新功能必须带最小可执行测试或验证脚本。
- 不提交 `workspace/`、密钥、客户资料和本地模型输出。

## 当前阶段

优先完成单文件 CLI 闭环：导入、解析、原子证据、Wiki 草稿、审核与审计。Web 工作台、向量检索与多用户能力后置。

