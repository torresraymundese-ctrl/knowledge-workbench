# 知识工作台规则（唯一真相）

本文件定义系统与所有开发智能体必须遵循的知识规则。`AGENTS.md` 和 `CLAUDE.md` 只作为工具入口，不得与本文件冲突。

## 1. 数据层级

1. 原始资料：内容寻址的只读副本，任何流程不得覆盖或改写。
2. 解析单元：保留文档原有定位信息的文本单元。
3. 原子证据：能够独立审核、引用和撤销的最小原文片段。
4. Wiki 草稿：由一条或多条证据支持的待审核知识页面修订。
5. 正式知识：人工审核通过、对查询默认生效的页面修订。

## 2. 原子证据硬约束

每条原子证据必须包含：

- 原始文件版本 ID 与 SHA-256；
- 非空原文片段；
- 解析器或提取模型；
- 审核状态；
- 创建和更新时间。

定位信息尽量包含：PDF 页码、Word 标题路径和段落序号、Excel 工作表与单元格范围、PPT 幻灯片号、Markdown 标题路径与行号。定位缺失不阻断入库，原文片段缺失必须阻断。

## 3. 状态语义

- 证据：`draft -> reviewing -> verified`，也可进入 `conflicted`、`deprecated`、`archived`。
- 页面修订：`draft -> reviewing -> verified`，也可进入 `rejected`、`superseded`、`archived`。
- `conflicted` 仅表示两个或多个证据在同一适用范围内互相矛盾。
- 已发布页面的变更必须创建新修订；新修订通过前，旧 `verified` 修订继续生效。
- 新修订发布后，受影响的下游知识标记为 `needs_revalidation`。

## 4. 引用规则

- Wiki 中的事实性结论必须引用原子证据 ID。
- 没有证据支持的模型输出只能作为建议或待调查项，不得表述为正式事实。
- 证据被撤销、过期或权限改变时，引用它的页面必须进入复核队列。

## 5. 密级与模型路由

- `public`：允许调用配置好的云模型。
- `internal`：默认只允许本地处理；每次云调用需人工显式授权并写审计日志。
- `confidential`：禁止云调用。
- `restricted`：禁止云调用，并限制内容导出与复制。
- 模型路由由代码校验，不得仅依赖提示词约束。
- 项目禁止调用 `qwen2.5:7b-instruct`；除非用户以后明确修改此决定，否则本地已安装不等于允许使用。

## 6. 增量与审计

- SHA-256 相同的资料不得重复创建知识。
- 同一路径内容改变时创建新的文件版本，保留历史版本。
- 导入、提取、状态变更、发布、模型调用和失败重试都必须记录审计事件。
- 自动流程不得静默覆盖人工修改。

## 7. 输出与兼容性

- Wiki 使用 UTF-8 Markdown 和 YAML Frontmatter。
- 页面保持 Obsidian 可读，但核心业务状态以 SQLite 为准。
- 所有自动生成页面必须带生成器、生成时间、修订号和证据引用。
