# 知识工作台规则（唯一真相）

本文件定义系统与所有开发智能体必须遵循的知识规则。`AGENTS.md` 和 `CLAUDE.md` 只作为工具入口，不得与本文件冲突。

## 1. 数据层级

1. 资料地图：对操作者明确指定目录进行本地只读预读后形成的文件清单、文档说明卡和候选关系；不是正式知识。
2. 原始资料：内容寻址的只读副本，任何流程不得覆盖或改写。
3. 解析单元：保留文档原有定位信息的文本单元。
4. 原子证据：能够独立引用和撤销的最小原文片段。
5. Wiki 草稿：由一条或多条证据支持的待审核知识页面修订。
6. 正式知识：人工审核通过、对查询默认生效的页面修订。

### 1.1 正式导入前的只读预读

- 资料地图扫描必须由操作者明确指定根目录和 actor；不得自动扩展到根目录之外。
- 预读只允许在本机解析正文以生成可理解的说明卡，不复制文件、不写入 `workspace/raw/`、不创建解析单元、原子证据、Wiki 或模型任务。
- 预读不得调用云模型；疑似账号密码、密钥清单、临时文件和备份文件只登记安全元数据，不读取正文。
- 资料地图必须保留文件夹层级、内容哈希、文件状态、重复组和版本候选，并清楚标注系统不确定项。
- 只有经范围确认的资料才可进入正式准入和现有 SHA-256 导入链路；资料地图中的“范围内”不等于已经导入或成为正式知识。

## 2. 原子证据硬约束

每条原子证据必须包含：

- 原始文件版本 ID 与 SHA-256；
- 非空原文片段；
- 解析器或提取模型；
- 审核状态；
- 创建和更新时间。

定位信息尽量包含：PDF 页码、Word 标题路径和段落序号、Excel 工作表与单元格范围、PPT 幻灯片号、Markdown 标题路径与行号。定位缺失不阻断入库，原文片段缺失必须阻断。

## 3. 状态语义

- 证据：`draft -> reviewing -> verified`，也可进入 `conflicted`、`deprecated`、`archived`。该状态只表示提取与回源技术校验，不表示业务权威性或适用范围已经批准。
- 页面修订：`draft -> reviewing -> verified`，也可进入 `rejected`、`superseded`、`archived`。
- `conflicted` 仅表示两个或多个证据在同一适用范围内互相矛盾。
- 已发布页面的变更必须创建新修订；新修订通过前，旧 `verified` 修订继续生效。
- 新修订发布后，受影响的下游知识标记为 `needs_revalidation`。

### 3.1 技术校验与业务审核分离

- 原文非空、内容哈希、当前文件版本、解析定位、Schema 和镜像一致性由系统自动校验，不要求人工逐条盖章。
- 经资料地图明确准入并导入的资料，只有全部依据技术校验通过后才可进入检索；系统自动写入兼容状态和审计，不得再要求人工逐条把 `draft` 点成 `verified`。
- 人工审核对象是资料范围、文档权威性、版本适用性、异常与冲突，以及 Wiki 的业务结论。
- 普通原子证据不得默认进入人工业务审核队列；只有技术校验失败、存在冲突或被业务结论引用时才按需回源。
- `verified` 证据在界面中必须显示为“技术校验通过”，不得显示为“业务已批准”。
- 开发样例、候选资料和正式业务资料必须分开统计；开发样例不得计入企业黄金基线或默认业务知识范围。

### 3.2 资料类型与主题知识页

- 正式准入资料还必须按用途标记为：业务资料、外部政策、技术设计、填报模板、示例演示、过程记录。该标记用于检索路由，不改变原始文件。
- 普通业务问答默认只使用业务资料和外部政策；只有问题明确涉及数据库、接口或架构时才加入技术设计。
- 填报模板、示例演示和过程记录只有在使用者明确询问对应内容时才可进入答案，不得被当作现行规则。
- 单文件 Wiki 草稿继续保留为解析追溯产物，但不进入默认业务审核队列。
- 面向业务审核的主题知识页必须引用至少两份不同资料，只能使用当前版本、技术校验通过、范围内、非受限且未被替代的证据。
- 首版主题知识页只允许逐字摘录和可读来源说明；系统不得在人工审核前把跨资料推断写成已确认结论。
- 主题页的每个章节必须单独声明其证据 ID，避免把整页全部依据错误地赋给每一个结论。

## 4. 引用规则

- Wiki 中的事实性结论必须引用原子证据 ID。
- 没有证据支持的模型输出只能作为建议或待调查项，不得表述为正式事实。
- 证据被撤销、过期或权限改变时，引用它的页面必须进入复核队列。
- 默认问答只使用已标为正式业务资料、已纳入业务范围且未被新版本替代的内容；开发样例和范围未确认资料不得用于企业答案。
- 问答顺序固定为 Wiki 优先、人工确认的实体关系扩展、原子证据兜底；任一步证据不足、存在冲突或权限受限时必须用普通业务文字说明。
- 面向使用者的答案先说明“找到什么、为什么相关、依据在哪里”，不得以内部 ID、JSON 定位或英文状态码代替解释。

## 5. 密级与模型路由

- `public`：允许调用配置好的云模型。
- `internal`：默认只允许本地处理；每次云调用需人工显式授权并写审计日志。
- `confidential`：禁止云调用。
- `restricted`：禁止云调用，并限制内容导出与复制。
- 模型路由由代码校验，不得仅依赖提示词约束。
- 正文中出现高置信账号口令、密钥或访问令牌时，即使文件名正常，也必须阻断预读正文、隔离已导入资料，并且审计中不得记录具体凭据值。
- 项目禁止调用 `qwen2.5:7b-instruct`；除非用户以后明确修改此决定，否则本地已安装不等于允许使用。

## 6. 增量与审计

- SHA-256 相同的资料不得重复创建知识。
- 同一路径内容改变时创建新的文件版本，保留历史版本。
- 导入、提取、状态变更、发布、模型调用和失败重试都必须记录审计事件。
- 自动流程不得静默覆盖人工修改。

## 7. 输出与兼容性

- Wiki 使用 UTF-8 Markdown 和 YAML Frontmatter。
- 页面保持 Obsidian 可读，但核心业务状态以 SQLite 为准。
- 所有自动生成页面必须带生成器、生成时间、修订号和证据引用。
