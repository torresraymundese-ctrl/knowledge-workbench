# Claude Code 项目入口

请先完整阅读 `KNOWLEDGE_RULES.md` 与 `AGENTS.md`。本文件只声明 Claude Code 的协作边界：

- 默认执行只读分析、测试与代码审查，不与其他智能体同时修改同一文件。
- 未经明确授权，不把 `internal`、`confidential` 或 `restricted` 资料发送给云端模型。
- 发现规则与实现冲突时，以 `KNOWLEDGE_RULES.md` 为准并报告差异。
- 不复制或移植 `nashsu/llm_wiki` 等 GPL 项目源码。

